get:
  operationId: getRoom
  summary: Get a specific room object.
  responses:
    200:
      description: OK
      content:
        application/json:
          schema:
            type: object
            required:
              - room
            properties:
              metrics:
                type: object
                properties:
                  $ref: "../../schemas/room.yaml"