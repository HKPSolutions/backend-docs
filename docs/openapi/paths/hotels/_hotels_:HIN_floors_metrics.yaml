get:
  operationId: getFloorsMetrics
  summary: Get metrics for all floors.
  responses:
    200:
      description: OK
      content:
        application/json:
          schema:
            type: object
            required:
              - metrics
            properties:
              metrics:
                type: object
                properties: 
                  $ref: "../../schemas/metrics.yaml"
                